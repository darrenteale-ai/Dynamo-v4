# Not needed in production - the backend serves the built frontend from the same origin.
# Only used for local dev if you run the Vite dev server standalone; vite.config.js already
# proxies /api to http://localhost:4000.
